// Centralized API base URL.
// TODO(Phase 5 / pre-TestFlight): swap to an env-driven config (Expo's app.config.ts + EXPO_PUBLIC_API_BASE)
// so dev / staging / prod resolve correctly. For now, this is the dev box IP.
export const API_BASE = 'http://192.168.0.64:3000'
